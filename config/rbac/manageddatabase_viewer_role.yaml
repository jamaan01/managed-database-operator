# This rule is not used by the project managed-database-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to demo.example.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: managed-database-operator
    app.kubernetes.io/managed-by: kustomize
  name: manageddatabase-viewer-role
rules:
- apiGroups:
  - demo.example.com
  resources:
  - manageddatabases
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - demo.example.com
  resources:
  - manageddatabases/status
  verbs:
  - get
